rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Everything is handled by the backend (Admin SDK)
    // The web client does NOT fetch from Firestore directly.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
